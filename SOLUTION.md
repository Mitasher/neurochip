# Текущее решение — кратко

Сравниваем устойчивость к SEU (однобитовый радиационный сбой) двух архитектур на
двух алгоритмах. Подробности — в [docs/03_experiment.md](docs/03_experiment.md),
история правок — в [changes.md](changes.md).

## Что сделано

- **Вход (общий):** тернарная матрица `W{-1,0,1}` (4×16) и бинарный вектор `x{0,1}`.
  Эталон: `W·x = [1,-1,2,3]` → класс `3`, спайкуют нейроны `{0,2,3}`.
- **Две платформы:**
  - фон-Нейман — numpy-модель «обычного МК» (float32 / int8, аккумулятор, счётчик цикла);
  - нейроморф — **реальный эмулятор чипа AltAI** (golden model).
- **Два эксперимента в выводе:**
  - Эксп. 1 — линейная алгебра `y = W·x` (метрика: отклонение выхода);
  - Эксп. 2 — перцептрон `argmax / step(W·x−θ)` (метрика: смена решения).
- **Сбой в железе:** бит веса / аккумулятора / счётчика (МК),
  бит тернарного синапса (чип), бит мембраны (numpy-модель).
- **На чипе:** битфлип синапса → пересборка placed-конфига под новые
  веса → исполнение на чипе. Каждый прогон — в отдельном процессе.
- **Прозрачность:** в `results/seu_report.json` для каждого прогона.
  `вход → какой бит перевёрнут (старое→новое) → выход до/после → сменилось ли решение`.

## Результат (seed=42, нейроморф — на чипе)

| | макс. откл. (эксп.1) | inf | смена решения (эксп.2) | fatal | crash |
|---|---:|---:|---:|---:|---:|
| фон-Нейман float32 | 130× | 10.7% | 11.3% | 3.3% | 7.3% |
| фон-Нейман int8 | 68000× | 19.0% | 31.0% | 13.7% | 5.3% |
| **нейроморф AltAI (ЧИП)** | **0.58×** | **0%** | **11.1%** | **0%** | **0%** |

- Главное отличие — **в характере отказа**, а не в частоте: на чипе сбой
  ограничен по величине (`≤0.58×`), (`0% fatal/crash`, ни одного
  `inf`); у МК — часто катастрофичен (до 68000×, до 13.7% `inf`, 5–7% крахов потока
  управления, которых у нейроморфа нет по построению).
- Доп. (numpy-модель мембраны): транзиент сам затухает за ~5 тиков (85% случаев).

## Что НЕ хайп (ограничения)

- фон-Нейман — numpy-имитация, не настоящая прошивка.
- На чипе 18 инъекций в синапс, а не 100.
- Инъекция в мембрану и самовосстановление — на numpy-модели (gm не даёт состояние
  нейрона во время счёта), помечено явно.
- Бьём только в синапс/мембрану нейроморфа; адресные регистры/маршрутизацию ядер не трогаем.

## Что улучшить, чтобы стало хайп

- **Реальный МК в эмуляторе.** Поднять Renode/QEMU (Cortex-M), скомпилировать оба
  алгоритма как прошивку и бить прямо в RAM/регистры/PC — тогда обе стороны «в железе».
- **Больше прогонов на чипе.** Кэшировать сборки по хэшу весов и/или гонять
  инъекции параллельно → довести чип с 18 до 100+ без долгого ожидания.
- **Точечная правка синапса на чипе** вместо пересборки (разобрать раскладку ядер
  Placer'а / `reload_neuron_synapses`) → быстрее и реалистичнее.
- **Шире мишени на чипе:** биты маршрутизации, axon/ATH-конфиг, счётчик тиков —
  показать классы сбоев за пределами весов.=

